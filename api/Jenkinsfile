pipeline {
    agent {
        docker{
            image 'ruby'
        }
    }

    stages {
        stage('prep'){
            steps {
                sh 'bundle install'
            }
        }
        stage('testing') {
            steps {
                sh 'cd api && rspec'
            }
        }
    }
}
